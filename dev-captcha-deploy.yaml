---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: captcha-api
spec:
  selector:
    matchLabels:
      app: captcha-api
  template:
    metadata:
      labels:
        app: captcha-api
    spec:
      containers:
      - name: captcha-api
        image: registry.tech1a.co:5000/captcha:{{tag}}
        imagePullPolicy: Always
        resources:
          limits:
            memory: "512Mi"
            cpu: "100m"
        ports:
        - containerPort: 5000
      hostAliases:
      - ip: "172.30.72.101"     
        hostnames:
        - "idp.tech1a.co"  
      nodeSelector:
        nodetype: worker
      imagePullSecrets:
      - name: tech1a-secret
--- 
apiVersion: v1
kind: Service
metadata:
  name: captcha-api
spec:
  selector:
    app: captcha-api
  ports:
  - name: http
    port: 8080
    targetPort: 5000
  - name: https
    port: 443
  type: LoadBalancer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: captcha-api
  annotations:
    kubernetes.io/ingress.class: "nginx"
    #nginx.ingress.kubernetes.io/backend-protocol: "http"   
spec:
  tls:
  - hosts:
      - tech1a.co
    secretName: tech1a-tls

  rules:
    - host: dev-captcha.tech1a.co
      http:
        paths:
          - path: /swagger(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: captcha-api
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: captcha-api
                port:
                  number: 8080                